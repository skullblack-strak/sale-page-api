package routes

import "github.com/gofiber/fiber/v2"

func About(router fiber.Router) {
	// app.Get("/", func(c *fiber.Ctx) error {
	// 	return c.SendString("path => " + c.Path())
	// })
	// app.Post("/login",    handlerLogin)
}
